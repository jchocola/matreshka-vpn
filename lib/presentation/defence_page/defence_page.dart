import 'package:flutter/material.dart';

class DefencePage extends StatelessWidget {
  const DefencePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('DefencePage'),),
    );
  }
}
